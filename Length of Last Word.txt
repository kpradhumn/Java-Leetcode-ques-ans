//58.Length of Last Word

class Solution {
    public int lengthOfLastWord(String s) {
        String ss = s.trim();
        int length = ss.length();//11
        int lastIndexOfSpace = ss.lastIndexOf(" ");//5
        return (length - lastIndexOfSpace -1);

    }
}